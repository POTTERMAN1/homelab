apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: ngnix-app
spec:
  type: LoadBalancer
  selector:
    app: nginx-app
  ports:
    - name: http
      port: 90
      targetPort: 80
    - name: https
      port: 444
      targetPort: 443

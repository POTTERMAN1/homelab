desec_api_token: "{{ vault_desec_api_token }}"
zerotier_network_id: "{{ vault_zerotier_network_id }}"

seafile_db_password: "{{ vault_seafile_db_password }}"
seafile_client_id: "{{ vault_seafile_client_id }}"
seafile_client_secret: "{{ vault_seafile_client_secret }}"
seafile_secret_key: "{{ vault_seafile_secret_key }}"

authentik_pg_password: "{{ vault_authentik_pg_password }}"
authentik_secret_key: "{{ vault_authentik_secret_key }}"
authentik_email_host: "{{ vault_authentik_email_host }}"
authentik_email_port: "{{ vault_authentik_email_port }}"
authentik_email_username: "{{ vault_authentik_email_username }}"
authentik_email_password: "{{ vault_authentik_email_password }}"
autentik_email_from: "{{ vault_autentik_email_from }}"

firefly_static_cron_token: "{{ vault_firefly_static_cron_token }}"
firefly_db_password: "{{ vault_firefly_db_password }}"
firefly_app_key: "{{ vault_firefly_app_key }}"

pihole_api_password: "{{ vault_FTLCONF_pihole_webserver_password }}"
paperless_db_password: "{{ vault_paperless_db_password }}"
paperless_secret_key: "{{ vault_paperless_secret_key }}"
paperless_client_id: "{{ vault_paperless_client_id }}"
paperless_client_secret: "{{ vault_paperless_client_secret }}"
